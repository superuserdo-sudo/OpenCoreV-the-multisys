# POS-pre-open-systerm

pos(pre open system) is an open source system form scratch and use an micro kernel made by "Adyasha Nanda"
and the os made by me(superuser-do) also the init is sysVinit use the corefs or the core fole system as the dirs
